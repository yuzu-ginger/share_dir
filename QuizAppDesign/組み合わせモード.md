## 組み合わせモード
このモード特有の設定を示す。

## View
```csharp
// 選択されているボタン(1つ目)
private Button? _selectedButton;

// 問題を更新する
private void UpdateQuestion()
{

}

// 1つ目のボタンをクリック
private void SelectFirstItem(Button button)
{
    // ボタンを選択した状態にする
    _selectedButton = button;
}

// 2つ目のボタンをクリック
private void SelectSecondItem(Button secondButton)
{
    // TODO: 1つ目と2つ目がペアか正誤判定をする
    bool correct = _session.isMatched(_selectedButoon, secondButton);
    // 正誤判定を表示する
    ShowJudgeResult(correct);
}

// ジャッジ結果を表示
private void ShowJudgeResult(bool correct)
{
}
```
## Session
- 組み合わせ判定
- 完了判定
- 問題リストを持っている